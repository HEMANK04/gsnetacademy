{
  "name": "gs-net-academy",
  "private": true,
  "description": "GS Net Academy — static HTML site. npm is only needed if you want to rebuild css/tailwind.css.",
  "scripts": {
    "css": "tailwindcss -i css/_tw-src.css -o css/tailwind.css --minify",
    "build": "python3 build/pages.py && npm run css"
  },
  "devDependencies": { "tailwindcss": "^3.4.0" }
}
